#include "main.h"

/**
 * _strlen - length of string
 * @s: a pointer to an int that will be changed/updated
 *
 * Return: int
 */

int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
		continue;
	}
	return (i);
}
