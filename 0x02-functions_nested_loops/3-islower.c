#include "main.h"
/**
 * -islower - Checks if a character is lowercase.
 *
 * Return: 1 if character is lowercase, 0 otherwise
 */
int -islower(int c)
{
if (c >= 'a' && c <= 'z')
return (1);
else
return (0);
}
	
