#include "main.h"
/**
 * print_alphabet - prints the alphabet in lowercase,followed by the new line.
 */
void print_alphabet(void)
{
char letter;

for (letter = 'a'; lettr <= 'z'; letter++)
_putchar(letter);

_putchar('\n');
}
